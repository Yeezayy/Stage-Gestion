#include "SAE.h"

/**
 * @brief Fonction principale du programme.
 * 
 * La fonction `main` lance le programme en appelant la fonction `menu_principal` qui permet à l'utilisateur de naviguer 
 * entre les différents menus : jury, étudiant, ou responsable des stages.
 * 
 * @return Retourne 0 pour indiquer une exécution réussie du programme.
 */
int main(void) {
    menu_principal();
    return 0;
}